# v0.4.0 发布前回归记录

- 日期：2026-09-05；执行者：Codex；环境：macOS arm64。
- 升级基线：最近已发布的 v0.3.3；本轮修复基于 master `8acc4d3669923b1e53bac71b17877105467e02d6`。
- 被测源码：首轮为 `7834f73adcbc4e99f111471cca8152adc5264526`；重新提交前的增量复验范围见下文。测试后只整理文档和创建本地 tag；产品代码变化需重跑对应检查。
- 发布范围：GitHub 压缩包与安装脚本；本轮移除 npm 分发。用户已在本地提交与标记完成后授权发布。

## 首轮修复（7834f73a）

- 修复生产启动时新手引导抢先判断空工作区的问题：等待已有的工作区、书架加载完成状态，避免挡住正常导航。

- 更新检查、安装和应用失败按请求语言返回本地化提示，内部诊断使用英文日志；移除未使用的服务端成功文案字段。
- 修正 SubAgent E2E 对已取消的显式等待步骤的断言，保留并发流隔离、刷新恢复、最终结果唯一性检查。
- 为原生 Windows CI 补充 Project 路径、数据目录复制以及 v0.3.3 迁移测试。
- E2E 可直接使用解压发布包的二进制、前端和 Skills；沿用独立测试数据和确定性模型。
- 删除 npm 分发源文件、过期 SubAgent 设计稿、临时 PDF 研究产物；同步版本日期、Node 要求和中英文截图。

## 首轮验证记录（7834f73a）

| 检查 | 结果 |
| --- | --- |
| 根模块与 agent：go mod tidy、go test、go vet | 通过，完整发布构建复验通过 |
| 根模块与 agent：全量 go test -race -count=1 | 通过 |
| 更新 HTTP 与异步任务错误：中文、英文 | 通过；原始诊断不出现在用户错误中 |
| 安装脚本：首次安装、重复安装、配置保留、损坏包拒绝 | 通过；SHA-256 失败后原程序与配置仍可用 |
| ./scripts/build.sh | 通过 |
| Windows internal/app 测试包交叉编译 | 通过；不等同于原生执行 |
| README 两种语言图片路径与原图字节一致性 | 通过 |
| git diff --check v0.3.3 | 通过 |
| 完整前端测试、双语键检查 | 567 项 Vitest、21 项 Playwright 全部通过；4272 个键一致 |
| 五平台发布构建、产物清单、SHA-256 | 全部通过；生产前端启动烟测通过 |
| macOS arm64 解压发布包的完整 E2E | 修复后 19 项全部通过，包括写作、游戏、资料库、版本、权限、附件、并发恢复与 v0.3.3 升级 |

首次完整浏览器回归为 20 项通过、1 项失败。失败来自旧断言要求看到 task_wait；当前 runtime 在下一次模型调用前等待已关联子任务。更新断言后单独复验通过。另一次构建因常规构建同时清理 output 导致 Go 目录扫描失败，已改为串行执行。

实际发布包首次执行发现游戏规划和资料库的点击被新手引导遮挡：设置先完成、工作区尚未完成时自动弹出。修复使用现有 workspaceLoaded 和 booksLoaded 状态，新增组件回归覆盖加载前、已有书籍、确实空工作区三种状态。未通过跳过引导或强制点击来规避问题。

自动化使用 Chromium、中文/深色为默认，导航测试覆盖自身设定的响应式和语言场景。浏览器只读检查了用户现有写作与游戏页面；未改动用户内容、未停止或替换用户服务。E2E 使用 web/test-results/runtime 中的临时数据及本地确定性 Chat Completions 模型。

## 重新提交前复验

- 基线：本地旧标签 `v0.4.0` 指向 `7834f73a`；GitHub 最新发布仍为 v0.3.3，远端尚无 v0.4.0 标签。
- 范围：提交已有的子 Agent 写入与独立会话日志修复、编辑失败诊断、流式消息性能与会话控件调整；将遗漏在 Unreleased 的两条修复并入 v0.4.0。
- 文档补漏：统一 README 徽章、源码启动说明与 CONTRIBUTING 的 Go 1.26.6+ / Node.js 22.13+ 要求，补充中英文升级说明入口，并修正贡献指南中过时的导航、变更记录和服务启动约定。

| 检查 | 结果 |
| --- | --- |
| 根模块与 agent：go mod tidy、go vet ./... | 通过，依赖清单无变化 |
| 根模块与 agent：go test -race -count=1 ./... | 全量通过；包含子 Agent 写入、修改、独立日志、重开恢复与冷启动继续，以及编辑失败反馈的集成回归 |
| 前端 Vitest、双语键检查 | 580 项测试全部通过；4272 个键一致 |
| ./scripts/build.sh | 通过 TypeScript、生产前端及含内嵌前端的本机打包构建；Vite 仍有大体积 chunk 提示 |
| 主程序与更新器版本 | 构建产物均返回 0.4.0 |
| 其他平台主程序与更新器交叉编译 | macOS x64、Linux arm64/x64、Windows x64 全部通过；macOS arm64 由本机构建覆盖 |
| scripts/install_test.sh | 通过 |
| 版本、日期、README 本地链接、Go 格式、git diff --check v0.3.3 | 通过；Unreleased 为空，修复已归入 v0.4.0 |
| 现有页面检查 | 写作和游戏在 1280 与 390 像素宽度下无页面横向溢出，一级导航保持唯一高亮；写作覆盖空编辑区、长会话、执行过程及工具详情展开；观察到的控制台 error 为零 |

本次页面检查使用现有开发服务，未重启、替换或另启前后端，未发送模型请求或修改用户内容。工具详情折叠时未挂载结果正文，展开一个读取工具后只挂载一份正文。

本次未重跑会另启服务的完整 Playwright E2E 和生产包启动测试，也未重新生成五平台发布压缩包。上方首轮 E2E、压缩包及 SHA-256 结果只对应旧提交；正式发布时仍应运行 `scripts/build-github-release.sh v0.4.0`，现有标签发布工作流会执行该完整流程。

本次日志位于本机 `/tmp/denova-v040-retag.IjXoO4/`，其中保存源码校验值以确认提交前无产品代码漂移。

## 本机验证限制

- 当前机器不能原生执行 Windows、WSL 或 Linux；五平台交叉编译不能证明全部宿主行为。Windows CI 扩展需代码推送后运行。
- 本轮没有逐个调用真实语言和图像服务商；确定性模型测试覆盖应用链路，不代表各服务商线上可用性。
- 上述本机检查完成时尚未推送源码或 tag；后续获授权后的 GitHub 检查见下文。

日志位于本机 /tmp/denova-v040-verification/，属于临时验证证据，不随产品分发。

## GitHub 发布检查与补漏

- 原生 Windows 检查已在 `a97bdb27` 通过，覆盖文件系统、Project 路径、数据目录搬迁与 v0.3.3 迁移。
- 首次远端 E2E 的三路 SubAgent 隔离与恢复测试在完成三轮检查后耗尽默认 30 秒总预算。为该多步骤测试启用 Playwright 的 `test.slow()`，保留原断言与每步超时。
- 后续远端回归发现 Follow Up 与首次回复共用显示消息 ID，导致前端合并时覆盖前一轮回复；两轮正文仍在 canonical journal 中。已用本地回归复现，再以已有的运行轮次区分显示 ID，并覆盖主 Agent、子 Agent、思考内容和日志重载。技能加载卡片也使用准备阶段的运行及轮次身份，避免依赖异步事件到达顺序。
- 补强真实 Follow Up 集成测试的消息 ID 唯一性断言；浏览器回归继续检查两轮回复在完成及刷新后各出现一次。
- 两次失败版本的 Release 工作流均在上传前取消；修复提交重新标记后，由完整发布流程验证、构建和上传。最终状态以 GitHub 的 `v0.4.0` Release 及对应工作流为准。

发布阶段诊断日志与失败浏览器 trace 保存在本机 `/tmp/denova-v040-publish.GBtCCq/`。
