# v0.4.0 发布前回归记录

- 日期：2026-09-05；执行者：Codex；环境：macOS arm64。
- 升级基线：最近已发布的 v0.3.3；本轮修复基于 master `8acc4d3669923b1e53bac71b17877105467e02d6`。
- 被测源码：包含本记录的提交。测试后只允许整理本记录和创建本地 tag；产品代码变化需重跑对应检查。
- 发布范围：GitHub 压缩包与安装脚本；本轮移除 npm 分发。正式 Release 等待用户确认。

## 本轮修复

- 修复生产启动时新手引导抢先判断空工作区的问题：等待已有的工作区、书架加载完成状态，避免挡住正常导航。

- 更新检查、安装和应用失败按请求语言返回本地化提示，内部诊断使用英文日志；移除未使用的服务端成功文案字段。
- 修正 SubAgent E2E 对已取消的显式等待步骤的断言，保留并发流隔离、刷新恢复、最终结果唯一性检查。
- 为原生 Windows CI 补充 Project 路径、数据目录复制以及 v0.3.3 迁移测试。
- E2E 可直接使用解压发布包的二进制、前端和 Skills；沿用独立测试数据和确定性模型。
- 删除 npm 分发源文件、过期 SubAgent 设计稿、临时 PDF 研究产物；同步版本日期、Node 要求和中英文截图。

## 验证记录

| 检查 | 结果 |
| --- | --- |
| 根模块与 agent：go mod tidy、go test、go vet | 通过，完整发布构建复验通过 |
| 根模块与 agent：全量 go test -race -count=1 | 通过 |
| 更新 HTTP 与异步任务错误：中文、英文 | 通过；原始诊断不出现在用户错误中 |
| 安装脚本：首次安装、重复安装、配置保留、损坏包拒绝 | 通过；SHA-256 失败后原程序与配置仍可用 |
| ./scripts/build.sh | 通过 |
| Windows internal/app 测试包交叉编译 | 通过；不等同于原生执行 |
| README 两种语言图片路径与原图字节一致性 | 通过 |
| git diff --check v0.3.3 | 通过 |
| 完整前端测试、双语键检查 | 567 项 Vitest、21 项 Playwright 全部通过；4272 个键一致 |
| 五平台发布构建、产物清单、SHA-256 | 全部通过；生产前端启动烟测通过 |
| macOS arm64 解压发布包的完整 E2E | 修复后 19 项全部通过，包括写作、游戏、资料库、版本、权限、附件、并发恢复与 v0.3.3 升级 |

首次完整浏览器回归为 20 项通过、1 项失败。失败来自旧断言要求看到 task_wait；当前 runtime 在下一次模型调用前等待已关联子任务。更新断言后单独复验通过。另一次构建因常规构建同时清理 output 导致 Go 目录扫描失败，已改为串行执行。

实际发布包首次执行发现游戏规划和资料库的点击被新手引导遮挡：设置先完成、工作区尚未完成时自动弹出。修复使用现有 workspaceLoaded 和 booksLoaded 状态，新增组件回归覆盖加载前、已有书籍、确实空工作区三种状态。未通过跳过引导或强制点击来规避问题。

自动化使用 Chromium、中文/深色为默认，导航测试覆盖自身设定的响应式和语言场景。浏览器只读检查了用户现有写作与游戏页面；未改动用户内容、未停止或替换用户服务。E2E 使用 web/test-results/runtime 中的临时数据及本地确定性 Chat Completions 模型。

## 发布前仍需确认

- 当前机器不能原生执行 Windows、WSL 或 Linux；五平台交叉编译不能证明全部宿主行为。Windows CI 扩展需代码推送后运行。
- 本轮没有逐个调用真实语言和图像服务商；确定性模型测试覆盖应用链路，不代表各服务商线上可用性。
- 未推送源码或 tag，未创建或发布 GitHub Release。现有 tag 推送工作流会自动发布，需等用户确认再推送。

日志位于本机 /tmp/denova-v040-verification/，属于临时验证证据，不随产品分发。
